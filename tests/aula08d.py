# Using the emoji lib
# As it is an external lib, to install it, run the command" python -m pip install emoji"

# python -m pip install lib_name

import emoji

print(emoji.emojize('Python is :thumbs_up:'))
